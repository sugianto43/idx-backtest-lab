import { PlaceholderRoute } from "@/components/layout/PlaceholderRoute";

export default function DatasetsPage() {
  return (
    <PlaceholderRoute
      title="Datasets"
      description="Importing, validating, and browsing market-data datasets."
      ownedBy="TASK-010"
    />
  );
}
