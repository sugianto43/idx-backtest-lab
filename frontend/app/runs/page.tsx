import { PlaceholderRoute } from "@/components/layout/PlaceholderRoute";

export default function RunsPage() {
  return (
    <PlaceholderRoute
      title="Runs"
      description="Creating backtest runs and reviewing their result artifacts and metrics."
      ownedBy="TASK-010"
    />
  );
}
